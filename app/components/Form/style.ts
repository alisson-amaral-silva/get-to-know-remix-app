import { styled } from "@stitches/react";

export const FormWrapper = styled('form', {
    width: '100%',
    maxWidth: 480,
    padding: 64,
    display: 'flex',
    flexFlow: 'column',
    alignItems: 'center',
    justifyContent: 'center'
  })